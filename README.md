# NextWordPredict
This project implements a Next Word Prediction system using Deep Learning. The model learns patterns from text and predicts the most probable next word based on a sequence of previous words.
